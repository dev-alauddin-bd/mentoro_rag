version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
    # Uncomment the following lines if you need live‑reload during development
    # volumes:
    #   - .:/app
    #   - /app/__pycache__
